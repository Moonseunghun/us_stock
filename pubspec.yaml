name: us_stock
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.3.0
  freezed_annotation: ^2.2.0
  provider: ^6.1.2
  json_annotation: ^4.8.1
  hive_flutter: ^1.1.0
  hive: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
  build_runner: ^2.3.3
  freezed: ^2.2.0
  json_serializable: ^6.6.2  # ✅ 여기 주목! analyzer <5.0.0 지원 마지막 버전
  hive_generator: ^2.0.1


flutter:
  uses-material-design: true
